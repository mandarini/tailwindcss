{
  "name": "internal-integrations",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "integration-test": "vitest --no-file-parallelism"
  },
  "devDependencies": {
    "dedent": "1.5.3",
    "fast-glob": "^3.3.2",
    "kill-port": "^2.0.1"
  },
  "nx": {
    "targets": {
      "integration-test": {
        "dependsOn": [
          "^build",
          {
            "projects": "@tailwindcss/root",
            "target": "build"
          }
        ]
      }
    }
  }
}
